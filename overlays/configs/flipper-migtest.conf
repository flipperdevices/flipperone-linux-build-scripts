# flipper migrate hard-test config (add/add conflict case).
# v2 ships this file; the user creates /etc/flipper-migtest.conf too with a different
# 'setting=' line, so migrate sees an add/add and must produce a conflict on that line.
setting=v2
shared=common
