# Mount binfmt_misc eagerly, BEFORE systemd-udev-trigger fires the coldplug
# uevent storm into PID 1's queue. The mount has no device dependency, so it
# can run while PID 1's main loop is still idle.
[Unit]
After=systemd-modules-load.service
Before=systemd-udev-trigger.service
