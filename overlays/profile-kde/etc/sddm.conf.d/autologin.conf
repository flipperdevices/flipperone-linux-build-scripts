# migrate-test-v2: v2 adds this comment at the top; the user edits Session= at the bottom.
# The two edits do not overlap, so migrate must AUTO-MERGE them with no conflict.
[Autologin]
User=user
Session=plasma
